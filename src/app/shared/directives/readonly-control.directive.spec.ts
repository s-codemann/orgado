import { ReadonlyControlDirective } from './readonly-control.directive';

describe('ReadonlyControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadonlyControlDirective();
    expect(directive).toBeTruthy();
  });
});
